# DESIGN.md

Визуальный источник правды пилота. Продукт — [ARCHITECTURE.md](ARCHITECTURE.md). Этот файл — как должно **выглядеть**.

Направление: **вечерняя диспетчерская**. Спокойный тёмный экран для менеджеров командировок, не лендинг и не кислотный dark.

## Палитра

Тёплый графит, не `#000` и не белый. Акцент — приглушённый янтарь (потом служебные карточки ИИ, §10.2).

| Роль | oklch | Зачем |
| --- | --- | --- |
| фон | `0.19 0.018 75` | страница |
| карточка | `0.24 0.020 75` | шапка, панели, карточки входа |
| текст | `0.93 0.015 80` | основной |
| приглушённый | `0.68 0.020 75` | подписи |
| янтарь | `0.78 0.12 75` | кнопки, фокус, акцент |
| янтарь на кнопке | `0.18 0.02 75` | текст на янтарном |
| рамка | `0.34 0.018 75` | линии |

Тема по умолчанию тёмная. Сток shadcn (белый `:root` / серый `.dark`) не использовать как есть.

## Шрифты

Бесплатные, OFL, кириллица, `next/font/google`.

- **Unbounded** — заголовки страниц и имена на карточках входа.
- **Golos Text** — остальной интерфейс.
- Моноширинный — `ui-monospace`, не Geist Mono.

Нельзя: Inter, Geist, Roboto, Arial как основной набор.

## Motion

Только CSS / уже подключённый `tw-animate-css`.

Нужны: появление четырёх карточек входа (stagger 0 / 40 / 80 / 120 ms), hover и press у карточек и кнопок.

`prefers-reduced-motion: reduce` — без движения.

Нельзя: Framer Motion, кастомный курсор, сыплющиеся частицы.

## Прокрутка

Системный белый ползунок со стрелками на концах не использовать: на Linux в Chromium он выглядит как чужой Windows-виджет.

Везде, где крутится контент (страница, список диалогов, лента, textarea, широкая таблица):

- без кнопок ▲▼ на концах полосы;
- тонкая капсула (~8px), не широкий «рельс»;
- дорожка почти в цвет фона;
- бегунок — тёплый графит; при наведении чуть к янтарю, без кислотного glow.

Стили глобальные, не по одному экрану.

## Антипаттерны

- Белый шаблон «из коробки».
- Фиолетовый градиент на белом.
- Кислотный neon dark (яд-зелёный на чёрном).
- Новая UI-библиотека сверх shadcn + Tailwind.
